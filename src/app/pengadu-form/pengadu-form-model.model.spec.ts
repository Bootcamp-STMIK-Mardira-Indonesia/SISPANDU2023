import { PengaduFormModel } from './pengadu-form-model.model';

describe('PengaduFormModel', () => {
  it('should create an instance', () => {
    expect(new PengaduFormModel()).toBeTruthy();
  });
});
